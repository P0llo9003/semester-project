server:
  port: 9090

spring:
  main:
      web-application-type: reactive
  cloud:
    gateway:
      server:
        webflux:
          routes:
          - id: usuarios-service
            uri: http://localhost:9091
            predicates:
            - Path=/usuarios/**